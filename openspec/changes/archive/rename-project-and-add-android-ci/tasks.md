# Tasks

- [x] Rename the Gradle root project to `pulse-fit-engine`.
- [x] Update the Android namespace and application ID to `com.astor.pulsefitengine`.
- [x] Update the launcher branding to `Pulse Fit Engine`.
- [x] Add a GitHub Actions workflow that builds the debug APK and uploads it as an artifact.
- [x] Update `.gitignore` for generated Android and local tooling files.
